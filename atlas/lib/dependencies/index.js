/**
	Dependencies is a tool to collect dependencies from multiple packages.json files
	and installes them in a parent node-modules directory, within a root-dir, 
	essentially allowing you to run npm install once, for all applications 
	bellow the root-dir
*/